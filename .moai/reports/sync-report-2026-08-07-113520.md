# Sync report — SPEC-KEYCHAIN-001 — 2026-08-07 11:35:20

Branch `feature/SPEC-KEYCHAIN-001`, 3 commits on `b8b3259`. Backup:
`.moai-backups/sync-2026-08-07-113520/`.

## Synced

- `spec.md` — HISTORY entry for the partly-verified v1.0.0; three stale Dockerfile assertions marked
  superseded by `fc19a07` (§HISTORY, §2.4 table, §5).
- `plan.md` — the "Absent, and this SPEC must supply it" row for `params.py`/`settings.py`.
- `acceptance.md` — verification-status table reworked to per-criterion real state.
- `README.md`, `.moai/project/tech.md`, `.moai/project/product.md` — already synced by `9226cc2`
  (T11). Not touched here.

Status stays **`draft`**: definition-of-done items 2, 5 and 6 were never run.

## Phase 0.5 numbers

| | |
|---|---|
| Tests | 541 passed, 0 failed, 0 skipped (Python 3.11.14, pytest 9.72 s) |
| Coverage | 100.00% total, 843 statements, 0 missed; `keychain.py` 26 stmts at 100% |
| Per-file gate | passed on all six gated modules |
| Lint | `ruff check .` — All checks passed |
| CI floor | `MIN_PASSED` 469 → 541, from a real junitxml run |

## Commits

| | |
|---|---|
| `479d700` | feat(keychain): source declared secrets from the host credential store |
| `9226cc2` | docs(keychain): state the exposure this feature moves rather than removes |
| `5ffdfed` | test(keychain): acceptance coverage for the sourcing path and its gates |

## Open verification items

1. **AC-IMAGE, container half** — no `docker compose build`, no `import main` inside a built image.
   Docker daemon was not running. Definition of done item 5.
2. **AC-EXPOSE, live half** — `docker inspect` never observed printing the plaintext value;
   `/proc/1/environ` never read as `runner`; inspect-fails-after-`--rm` never observed. Only the
   documentation clause is discharged.
3. **T10 end to end** — launcher → `compose run` → `os.environ` → prelude never run as one chain.
4. **AC-DOCTOR** — asserted at source level, never executed; `--doctor` sits after the bootstrap.
5. **AC-LAUNCH under CI** — `tests/test_launcher_source.py` invokes bare `bash`: 3.2.57 here, 5.x on
   CI's Ubuntu runners, so N8 is not structurally guaranteed to be exercised under 3.2. Fix is to
   parametrise over both interpreters where present.
6. **AC-TRANSPORT** — the `ps -Ao args` clause not verified (no live session).
7. **AC-DEGRADE** — locked-keychain (rc 128) and cancelled-prompt rows not run.

## Superseded assertions

Six sites asserted a `Dockerfile` state the tree disproves, and a follow-up pass closed three more in
`plan.md` (T7, R3, the §1.2 priority row) that had been left contradicting `plan.md` §0. In every case
the original measurement is preserved and marked superseded by `fc19a07`, rather than rewritten — the
measurement was correct on the evidence available when it was taken. The `COPY` line is at
`Dockerfile:43` today; `:34` is retained wherever it labels the original observation.

T7 stays **High** priority despite its reduced scope: `keychain.py` is imported at `main.py` module
level (`main.py:49`), so omitting it from the `COPY` line is total failure at container start, not a
degraded feature.
